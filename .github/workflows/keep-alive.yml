name: Keep Streamlit app awake

on:
  schedule:
    - cron: "0 6,18 * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping app
        run: curl -sS -o /dev/null -w "HTTP %{http_code}\n" "https://sarb-hawkish-dove-llm-59qcmhguxqvpvab9kldq2q.streamlit.app/"
